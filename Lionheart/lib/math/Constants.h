#pragma once

#include <thirdparty/glm/glm.hpp>

namespace Constants
{
	static const float TWO_PI = glm::radians(360.0f);
	static const float PI = glm::radians(180.0f);
	static const float HALF_PI = glm::radians(90.0f);
}